let defaultColors = {
  buttonColors: {
    normal: 'green',
    cancel: 'red',
    download: 'orange',
    show: 'blue'
  },
  paginationColor: 'green'
};

export default Object.freeze(Object.assign({}, defaultColors));
