let defaultNumbers = {
  pageNumber: 1,
  minAnswerId: 0,
  exerciseLimitPerPage: 5,
  zoomSize: 19,
  scanDelayValue: 500,
  executionLimit: 200,
  timeoutTime: 2000,
  maxTrophyValue: 3
};

export default Object.freeze(Object.assign({}, defaultNumbers));
